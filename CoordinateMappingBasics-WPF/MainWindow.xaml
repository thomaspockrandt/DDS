<Window x:Class="Microsoft.Samples.Kinect.CoordinateMappingBasics.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Coordinate Mapping Basics" 
        Height="600" Width="950" 
        Closing="MainWindow_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="150" />
            <RowDefinition Height="*" />
            <RowDefinition Height="100" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <!--<WebBrowser x:Name="webBrowser" Grid.Column="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />-->
            <Viewbox Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Grid Name="CompositeImage">
                    <Image Source="Images\Berge.jpg" />
                    <Image Source="{Binding ImageSource}" Stretch="UniformToFill" />
                </Grid>
            </Viewbox>
        </Grid>
    </Grid>
</Window>
